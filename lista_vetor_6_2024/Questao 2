#include <stdio.h>

void main() {
    int vetor[4];
    int i;

    printf("Digite quatro numeros:\n");

    for (i = 0; i < 4; i++) {
        scanf("%d", &vetor[i]);
    }

    printf("em ordem inversa):\n");

    for (i = 3; i >= 0; i--) {
        printf("%d ", vetor[i]);
    }

}
